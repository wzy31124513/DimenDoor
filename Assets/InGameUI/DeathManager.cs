using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class DeathManager : MonoBehaviour
{
    // Start is called before the first frame update

    

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    void SetDeathTip()
    {


        /*public GameObject[] m_deathTips;
        public GameObject m_deathtip;
        public int m_length;
        m_deathTips = GameObject.FindGameObjectsWithTag("DeathTip");
        m_length = m_deathTips.Length;
        m_deathtip = m_deathTips[Random.Range(0, Length)];
        m_deathtip.SetActive(true); */
    }
}
